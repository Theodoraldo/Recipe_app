<!DOCTYPE html>
<html>
  <head>
    <title>RecipeApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.2/css/bulma.min.css">
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header><%= render partial: 'layouts/header' %></header>
    <div class="is-flex is-justify-content-center">
      <span class="subtitle has-text-success-dark has-background-success-light"><%= notice %></span>
      <span class="subtitle has-text-danger-dark has-background-danger-light"><%= alert %></span>
    </div>
    <%= yield %>
  </body>
</html>
